let famous_person: string = "Quaid -e- Azam";
let quote1: string = "Think a hundred times before you take a decision, but once that decision is taken, stand by it as one man.";
let message: string = `${famous_person} once said, "${quote1}"`;

console.log(message);
